

const PlayerControl = () => {
    return (
        <div>
            <button>Play</button>
            <button>Pause</button>
            <button>Stop</button>
        </div>
    );
}

export default PlayerControl;